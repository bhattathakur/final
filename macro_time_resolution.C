//This macro helps to combine the data from time information (date time runtime ) and resolution (resolution error)
{
  gROOT->Reset();
  char * resolution_data("combined_error_data"); //resolution error
  char * time_data("timedata.dat"); //date time runtime
  char * time_resolution_data("data_time_resolution.dat");

  ifstream input1(res
}
